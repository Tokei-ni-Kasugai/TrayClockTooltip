# TrayClockTooltip テストケース

現行挙動を確認するための手動テストケースです。デバッグ実行は行わず、ユーザー操作で確認します。

## 前提条件

- 最新の `TrayClockTooltip.exe` を使用する。
- テスト開始前に、実行中の `TrayClockTooltip.exe` を終了しておく。
- 各テストでは、アプリが二重起動していないことを確認する。
- NTP取得失敗を確認するケース以外では、Windows Time にNTPサーバが設定されていること。

## 基本起動

| ID | ケース | 手順 | 期待値 |
| --- | --- | --- | --- |
| ST-01 | 起動 | `TrayClockTooltip.exe` を起動する。 | メインウィンドウは表示されず、タスクトレイにアイコンが表示される。 |
| ST-02 | 単一起動 | 起動中に再度 `TrayClockTooltip.exe` を起動する。 | トレイアイコンは増えず、既存プロセスのみ動作する。 |
| ST-03 | トレイから終了 | トレイアイコンを右クリックし、`Exit` を選択する。 | アプリが終了し、トレイアイコンが消える。 |

## トレイhoverとフローティング時計

| ID | ケース | 手順 | 期待値 |
| --- | --- | --- | --- |
| HV-01 | hoverプレビュー表示 | トレイアイコンにマウスを乗せる。 | 標準ツールチップではなく、フローティング時計がツールチップ風に表示される。 |
| HV-02 | hoverプレビュー位置 | トレイアイコンにマウスを乗せる。 | トレイアイコン矩形が取得できる場合、アイコンの上に横中央揃えで表示される。取得できない場合はカーソル基準で表示される。 |
| HV-03 | 時計上では消えない | トレイアイコンにhoverし、そのまま上方向へ移動してフローティング時計上に乗せる。 | フローティング時計は表示されたまま。 |
| HV-04 | 時計から下方向へ戻る | hover表示後、フローティング時計からトレイアイコン方向へ下移動する。 | マウスが時計より下側かつアイコンX範囲内なら表示維持。 |
| HV-05 | 時計から横に外れる | hover表示後、フローティング時計から左または右へ外れる。 | フローティング時計が消える。 |
| HV-06 | 時計から上に外れる | hover表示後、フローティング時計から上へ外れる。 | フローティング時計が消える。 |
| HV-07 | アイコンX範囲から外れる | hover表示後、トレイアイコンの横幅範囲から外れる。 | フローティング時計が消える。 |
| HV-08 | ちらつき | トレイアイコン上でマウスを少し動かす。 | フローティング時計が過剰に再生成されたり、目立ってちらついたりしない。 |

## フローティング時計の固定表示とドラッグ

| ID | ケース | 手順 | 期待値 |
| --- | --- | --- | --- |
| FL-01 | hoverから固定 | hover表示中にトレイアイコンを左クリックする。 | フローティング時計が固定表示になる。 |
| FL-01A | 固定インジケータ | フローティング時計を固定表示する。 | フローティング時計の左右に小さなインジケータが表示される。 |
| FL-02 | 未移動の固定表示をhoverへ戻す | hover表示中にトレイアイコンを左クリックして固定表示にし、マウスを動かさずに再度トレイアイコンを左クリックする。 | フローティング時計は非表示にならず、内部的に非固定化される。マウスがhover維持範囲から外れると非表示になる。 |
| FL-03 | 左クリックで直接固定表示 | 非表示状態でトレイアイコンを左クリックする。 | フローティング時計が固定表示される。 |
| FL-04 | 固定時計のドラッグ | 固定表示中のフローティング時計をドラッグする。 | 時計がマウスに追従して移動し、ドロップ位置に残る。 |
| FL-05 | hover時計のドラッグ | hover表示中のフローティング時計をドラッグする。 | 時計が固定表示に切り替わり、ドラッグできる。 |
| FL-06 | hover時計の右クリック | hover表示中のフローティング時計を右クリックする。 | 時計が表示維持され、フローティング時計メニューが開く。 |
| FL-07 | Close | フローティング時計を右クリックし、`Close` を選択する。 | フローティング時計だけが非表示になる。アプリは終了しない。 |
| FL-08 | Exit | フローティング時計を右クリックし、`Exit` を選択する。 | アプリが終了し、トレイアイコンが消える。 |
| FL-09 | 移動済み固定表示を非表示 | フローティング時計をドラッグして移動し、その後トレイアイコンを左クリックする。 | 従来通りフローティング時計が非表示になる。 |

## トレイメニュー

| ID | ケース | 手順 | 期待値 |
| --- | --- | --- | --- |
| TM-01 | トレイメニュー表示 | トレイアイコンを右クリックする。 | トレイメニューが表示される。hoverプレビューが出ている場合は閉じる。 |
| TM-01A | トレイメニュー中のhover抑止 | トレイアイコンを右クリックしてメニューを表示し、メニュー表示中にアイコン上でマウスを動かす。または、いったんアイコンから外れて再度hoverする。 | メニュー表示中はフローティング時計のhoverプレビューが表示されない。メニューが閉じた後は通常のhover挙動に戻る。 |
| TM-02 | 通常メニュー | 現在の通知しきい値以上のずれが検出されていない状態でトレイメニューを開く。 | `NTP: refresh`、`Notify threshold`、`Startup`、`Exit` が表示される。 |
| TM-03 | トレイからNTP再取得 | トレイメニューの `NTP: refresh` を選択する。 | Windows Time設定のNTPサーバへ再問い合わせする。取得に成功し、ずれが現在の通知しきい値未満の場合は成功通知を表示する。 |
| TM-04 | 問い合わせ中のNTP項目 | NTP問い合わせ中にトレイメニューを開く。 | `NTP: refresh` は表示されるが、グレーアウトして選択できない。 |
| TM-05 | ずれ表示 | 現在の通知しきい値以上の時刻ずれを発生させ、NTP取得を行う。 | トレイメニュー先頭に表示専用の `Time: +...s (...)` または `Time: -...s (...)` が表示される。通常メニュー色で表示され、hoverしても選択色に変化しない。クリックしてもメニューは閉じない。 |
| TM-06 | トレイAdjust表示 | 現在の通知しきい値以上のずれが検出された状態でトレイメニューを開く。 | `Time: ...` の下に `Adjust Windows time (admin)` が表示される。選択すると管理者権限での時刻調整が開始される。 |
| TM-07 | Adjust非表示 | ずれが現在の通知しきい値未満の状態でトレイメニューを開く。 | 通知しきい値が `Off` でない限り、`Adjust Windows time (admin)` は表示されない。 |
| TM-08 | 高度な強制Adjust表示 | NTP取得後のずれが現在の通知しきい値未満の状態で、Shiftを押しながらトレイアイコンを右クリックする。 | `NTP: refresh`、`Adjust Windows time (admin)`、`Notify threshold`、`Startup`、`Exit` が表示される。Adjustを選択すると管理者権限での時刻調整が開始される。 |
| TM-09 | 問い合わせ中はAdjust非表示 | NTP問い合わせ中に、通常の右クリックとShift右クリックでトレイメニューを開く。 | `NTP: refresh` はグレーアウトし、古い状態に基づく `Adjust Windows time (admin)` は表示されない。問い合わせ中にしきい値を変更しても古いAdjust状態は復活せず、問い合わせ完了後の結果を新しいしきい値で評価する。 |
| TM-10 | Startupサブメニュー表示 | トレイアイコンを右クリックする。 | トレイメニューに `Startup` サブメニューが表示され、`Install for this user`、`Add this EXE to startup`、`Remove startup registration` が含まれる。 |
| TM-11 | Startupサブメニューのグレーアウト | 自動起動未登録、現在EXEが登録済み、自動起動登録先EXEが存在しない、自動起動登録先EXEが現在EXEと同一、自動起動登録先EXEが現在EXEと異なる、インストール先EXEが現在EXEと一致、Startup登録先がインストール先EXEではない、の各状態でトレイメニューを開く。 | 未登録時は `Remove startup registration` がグレーアウトする。`Add this EXE to startup` は、自動起動未登録、登録先EXEが存在しない、または登録先EXEが現在EXEと異なる場合だけ有効になる。`Install for this user` は、現在EXEがすでにインストール先EXEであり、インストール先EXEと一致している場合にグレーアウトする。この状態でStartup登録を戻す場合は `Add this EXE to startup` を使う。 |
| TM-12 | 高度なログフォルダ表示 | Shiftを押しながらトレイアイコンを右クリックし、`Open log folder` を選択する。 | `Open log folder` はShiftメニューでのみ表示され、現在使用するログフォルダを開く。NTP問い合わせ中でも表示される。 |
| TM-13 | 通知しきい値メニュー | トレイメニューを開き、`Notify threshold` の `1 sec`、`3 sec`、`5 sec`、`7 sec`、`10 sec`、`Off` をそれぞれ選択する。 | 次回メニュー表示時に選択値へチェックが付く。現在プロセスには即時反映される。フローティング時計メニューには `Notify threshold` は表示されない。 |
| TM-14 | 通知しきい値Off時のメニュー | `Notify threshold` を `Off` に設定し、NTP取得に成功させてから通常のトレイメニューとフローティングメニューを開く。 | 自動のずれ通知は表示されない。NTP問い合わせ中またはNTP未取得でない限り、Shiftなしで `Adjust Windows time (admin)` が表示される。 |

## 自動起動登録

| ID | ケース | 手順 | 期待値 |
| --- | --- | --- | --- |
| SU-01 | このユーザーにインストール | トレイメニューの `Startup` -> `Install for this user` を選択する。 | 現在のEXEを `%LOCALAPPDATA%\Programs\TrayClockTooltip\TrayClockTooltip.exe` にコピーし、コピー結果が現在EXEと一致することを検証する。起動元EXEの隣に配布用READMEが存在する場合はインストール先へコピーする。そのうえで、必要に応じて現在の通知しきい値引数を含めたパスを `HKCU\Software\Microsoft\Windows\CurrentVersion\Run\TrayClockTooltip` に書き込み、現在プロセス終了後にインストール先EXEを起動する。インストール先EXEからインストール成功通知を表示する。UACは表示されない。 |
| SU-02 | 現在のEXEを自動起動登録 | トレイメニューの `Startup` -> `Add this EXE to startup` を選択する。 | 現在のEXEパスと、必要に応じて現在の通知しきい値引数を `HKCU\Software\Microsoft\Windows\CurrentVersion\Run\TrayClockTooltip` に書き込み、成功通知を表示する。UACは表示されない。 |
| SU-03 | 自動起動登録削除 | トレイメニューの `Startup` -> `Remove startup registration` を選択する。 | `HKCU\Software\Microsoft\Windows\CurrentVersion\Run\TrayClockTooltip` を削除し、成功通知を表示する。UACは表示されない。 |
| SU-04 | 配置済みパスからの再インストール | `%LOCALAPPDATA%\Programs\TrayClockTooltip\TrayClockTooltip.exe` から起動し、`Startup` サブメニューを開く。 | 現在EXEはすでにインストール済みのため、`Install for this user` はグレーアウトする。Startup登録がない場合は `Add this EXE to startup` が有効になり、自動起動登録を戻せる。 |
| SU-05 | インストール失敗 | 配置先を利用不能または書き込み不可にして、`Install for this user` を選択する。 | アプリは動作継続し、失敗通知を表示する。既存の自動起動登録は意図的には削除されない。 |
| SU-06 | インストール先EXEが現在EXEと異なる | `%LOCALAPPDATA%\Programs\TrayClockTooltip\TrayClockTooltip.exe` に異なるEXEを配置し、別コピーからトレイメニューを開く。 | `Install for this user` は有効のまま。現在EXEでインストール先を更新できる。 |
| SU-07 | 廃止: 古いインストール引き継ぎケース | このケースは廃止。 | 以前のケースは現在のメニューグレーアウト条件と合わず、インストール/更新時の起動引き継ぎ確認としても重複するため、SU-10、SU-10A、SU-11、SU-12で確認する。 |
| SU-08 | 開発用インストール確認 | インストール先EXEが存在する状態で、インストール先以外のEXEを `--install-prompt` 付きで起動する。 | 起動前に、このEXEをインストールするか、インストール済みアプリを終了してこのEXEをポータブル動作として起動するか、キャンセルするかを確認する。インストールを選択した場合は `Install for this user` と同じ検証付きインストールと起動引き継ぎを行う。 |
| SU-08A | 開発用インストール確認でポータブル起動 | インストール先EXEが起動中の状態で、インストール先以外のEXEを `--install-prompt` 付きで起動し、`いいえ` を選択する。 | 起動中のインストール先インスタンスへ通常終了を依頼し、インストール先EXEは置き換えず、インストール先以外のEXEから起動を継続する。 |
| SU-09 | 開発用インストール確認スキップ | インストール先EXEが存在しない、または現在EXEがインストール先EXEである状態で `--install-prompt` 付き起動する。 | インストール確認は表示せず、通常起動を継続する。 |
| SU-10 | インストール先EXE起動中の更新 | インストール先EXEを起動した状態で、別のEXEを `--install-prompt` 付きで起動し、確認でインストールを選択する。 | 起動中のインストール先インスタンスへ通常終了を依頼し、短時間待ってからインストール先EXEを置き換える。終了できない場合は強制終了せず、インストール失敗扱いにする。 |
| SU-10A | 同じ起動元EXEが起動中のインストール確認 | インストール先以外のEXEを起動した状態で、同じEXEを `--install-prompt` 付きで起動し、`はい` を選択する。 | 起動中の起動元パスの既存インスタンスへ通常終了を依頼し、インストールを実行し、単一起動ミューテックスに弾かれずインストール先EXEを起動する。 |
| SU-11 | Startup登録先をインストール先へ戻す | インストール先EXEと現在EXEが同一バイナリの状態にする。別EXEを起動し、`Startup` -> `Remove startup registration`、続けて `Startup` -> `Add this EXE to startup` を実行してStartup登録を別EXEに向ける。その後、トレイメニューを開き、`Startup` -> `Install for this user` を選択する。 | `Install for this user` が有効になる。選択するとインストール先EXEの置き換えは不要だが、Startup登録を `%LOCALAPPDATA%\Programs\TrayClockTooltip\TrayClockTooltip.exe` に書き戻し、現在EXE終了後にインストール先EXEを起動し、成功通知を表示する。 |
| SU-12 | インストール後の起動元フォルダ削除 | 任意の一時フォルダにEXEを配置して起動し、そのEXEから `Install for this user` を選択する。インストール先EXEへ起動が引き継がれた後、起動元の一時フォルダを削除する。 | インストール先EXEが起動中でも、起動元の一時フォルダを削除できる。インストール先EXEは起動元フォルダをカレントディレクトリとして保持しない。 |
| SU-13 | Startup通知しきい値の継承 | Startup登録値に `--notify-threshold=5` を含めた状態で、しきい値引数なしの別EXEを起動する。 | 専用の設定ファイルや専用レジストリキーを作らず、登録済みの `5 sec` しきい値を現在プロセスが使用する。 |
| SU-14 | しきい値変更時に登録先パスを維持 | インストール先EXEを `--notify-threshold=5` 付きでStartup登録し、別のポータブルEXEを起動して `Notify threshold` を `3 sec` に変更する。 | ポータブルプロセスは即時に `3 sec` を使用する。Startup登録値はインストール先EXEパスを維持し、引数だけ `--notify-threshold=3` に変わる。 |
| SU-15 | しきい値変更時の既知オフセット再評価 | 既知のずれが現在のしきい値以上の状態でNTP取得後、そのずれより大きいしきい値へ変更する。逆に、既知のずれが現在のしきい値未満の状態でNTP取得後、そのずれより小さいしきい値へ変更する。 | 最新の既知NTPオフセットをもとに、トレイ/フローティングの `Adjust Windows time (admin)` 表示可否が即時再計算される。新しいしきい値がずれより大きい場合は非表示になり、小さい場合は表示される。しきい値を下げた結果ずれ状態になった場合は、ずれ通知も表示する。NTP再取得やアプリ再起動は不要。 |

## ビルドスクリプト

| ID | ケース | 手順 | 期待値 |
| --- | --- | --- | --- |
| BS-01 | ビルドのみ | `powershell -ExecutionPolicy Bypass -File .\src\build.ps1` を実行する。 | ビルドに成功し、`dist\TrayClockTooltip.exe` を出力する。アプリは起動しない。 |
| BS-02 | ビルド後起動 | `powershell -ExecutionPolicy Bypass -File .\src\build.ps1 -Run` を実行する。 | ビルドに成功し、ビルドしたEXEを起動する。 |
| BS-03 | ビルド後インストール確認付き起動 | `powershell -ExecutionPolicy Bypass -File .\src\build.ps1 -RunInstallPrompt` を実行する。 | ビルドに成功し、ビルドしたEXEを `--install-prompt` 付きで起動する。 |

## フローティング時計メニュー

| ID | ケース | 手順 | 期待値 |
| --- | --- | --- | --- |
| FM-01 | メニュー表示 | フローティング時計を右クリックする。 | フローティング時計メニューが表示される。 |
| FM-02 | 通常メニュー | 現在の通知しきい値以上のずれが検出されておらず、通知しきい値が `Off` ではない状態でメニューを開く。 | `Close`、区切り線、`Exit` が表示される。 |
| FM-03 | フローティングにNTP再取得なし | 現在の通知しきい値以上のずれが検出されていない状態でメニューを開く。 | `NTP: refresh` は表示されない。 |
| FM-04 | 問い合わせ中のフローティングメニュー | NTP問い合わせ中にフローティング時計メニューを開く。 | `NTP: refresh` は表示されない。 |
| FM-05 | Adjust表示 | 現在の通知しきい値以上のずれが検出された状態でフローティング時計メニューを開く。 | `NTP: refresh` は表示されず、`Adjust Windows time (admin)` が表示される。 |
| FM-06 | ずれ文字列は表示しない | 現在の通知しきい値以上のずれが検出された状態でフローティング時計メニューを開く。 | フローティング時計メニューは `Time: ...` を表示しない。 |
| FM-07 | 高度な強制Adjust表示 | NTP取得後のずれが現在の通知しきい値未満の状態で、Shiftを押しながらフローティング時計を右クリックする。 | `Adjust Windows time (admin)`、区切り線、`Close`、区切り線、`Exit` が表示される。Adjustを選択すると管理者権限での時刻調整が開始される。 |
| FM-08 | 問い合わせ中は強制Adjust非表示 | NTP問い合わせ中に、Shiftを押しながらフローティング時計を右クリックする。 | `NTP: refresh` も `Adjust Windows time (admin)` も表示されない。 |
| FM-09 | 高度なフローティングログフォルダ表示 | Shiftを押しながらフローティング時計を右クリックし、`Open log folder` を選択する。 | `Open log folder` はShiftメニューでのみ表示され、現在使用するログフォルダを開く。NTP問い合わせ中でも表示される。 |

## NTPと時計表示

| ID | ケース | 手順 | 期待値 |
| --- | --- | --- | --- |
| NT-01 | 起動時NTP取得 | アプリを起動する。 | Windows Timeの `NtpServer` を読み取り、そのサーバへUDP/123で問い合わせる。 |
| NT-02 | NTPオフセット利用 | NTP取得に成功した状態でフローティング時計を表示する。 | 日付部分と時刻部分はWindowsのユーザー設定形式に従い、`日付 時刻` の並びで、Windows時刻に取得オフセットを加味した時刻が表示される。Windowsの時刻形式が秒を含まない場合でも、秒は補完表示される。 |
| NT-03 | 手動再取得で更新 | 現在の通知しきい値以上のずれが検出されていない状態でトレイメニューの `NTP: refresh` を選択する。 | 新しく取得したNTPオフセットでアプリ時計が更新される。ずれが現在の通知しきい値未満の場合は成功通知を表示する。 |
| NT-04 | NTP取得失敗通知 | NTP取得できない状態で起動またはトレイメニューの `NTP: refresh` を実行する。 | NTP時刻を取得できず、Windowsシステム時計を使う旨の通知が表示される。以前のNTPオフセットは使用されない。 |
| NT-05 | NTP取得失敗時のメニュー | NTP取得失敗後にメニューを開く。 | トレイメニューは `NTP: refresh` に戻り、`Adjust Windows time (admin)` は表示されない。 |
| NT-06 | NTP参照先追従 | Windows Timeの `NtpServer` を変更し、トレイメニューの `NTP: refresh` を選択する。 | ハードコードされたサーバではなく、設定されたサーバに問い合わせる。 |
| NT-07 | ロック解除/ログオン時NTP取得 | アプリを起動したまま、同一Windowsセッションへログオンまたはロック解除する。 | NTP問い合わせを開始する。問い合わせ中の場合は二重に開始しない。 |
| NT-08 | ポータブル起動時の起動確認通知 | インストール先EXEが存在せず、Startup登録も存在しない状態で、インストール先以外のEXEから起動する。起動時NTP取得が成功し、現在の通知しきい値未満のずれにする。 | TrayClockTooltipが起動中であり、トレイアイコンにマウスを合わせると時計を表示できる旨の通知を表示する。インストール先EXEが存在する、Startup登録済み、NTP失敗、またはしきい値以上のずれがある場合、この起動確認通知は表示しない。 |

## ずれ通知と時刻調整

| ID | ケース | 手順 | 期待値 |
| --- | --- | --- | --- |
| AD-01 | ずれ通知表示 | 現在の通知しきい値以上の時刻ずれを発生させ、NTP取得を行う。 | 通知しきい値が `Off` でない限り、カスタム通知が表示され、Windows時刻がずれておりアプリ時計はNTP時刻を使う旨が示される。 |
| AD-01A | 通知しきい値Off時のずれ通知 | `Notify threshold` を `Off` に設定し、時刻ずれを発生させた状態で、起動時、ログオン時、ロック解除時の自動NTP取得を行う。 | 自動のずれ通知は表示されない。アプリ時計は取得したNTPオフセットを使い、NTP取得成功後は通常メニューから時刻調整できる。 |
| AD-02 | 通知自動クローズ | ずれ通知を表示し、待機する。 | 約5秒後に通知が閉じる。 |
| AD-03 | 通知クリックで調整 | ずれ通知をクリックする。 | `Adjust Windows time (admin)` 相当の処理が開始され、UACが表示される。 |
| AD-04 | トレイから調整 | ずれ検出状態で、トレイメニューの `Adjust Windows time (admin)` 項目を選択する。 | 昇格ヘルパープロセスのUACが表示される。 |
| AD-05 | フローティングAdjust | ずれ検出状態で、フローティング時計メニューの `Adjust Windows time (admin)` を選択する。 | 昇格ヘルパープロセスのUACが表示される。 |
| AD-06 | UACキャンセル | 時刻調整を開始し、UACをキャンセルする。 | アプリは動作継続する。キャンセルだけを理由に追加通知は出ない。ずれ状態は再取得または調整まで維持される。 |
| AD-07 | UAC承認かつ調整成功 | 時刻調整を開始し、UACを承認し、昇格ヘルパーを成功させる。 | 昇格ヘルパーがNTPを再取得し、その新しい時刻で `SetLocalTime` を実行する。メインアプリは調整メニューとずれ表示を隠し、成功通知を表示する。 |
| AD-08 | UAC待機時の時刻鮮度 | UAC表示後しばらく待ってから承認する。 | UAC前に取得した時刻ではなく、昇格ヘルパーが再取得した時刻が適用される。 |
| AD-09 | 昇格ヘルパー失敗 | 時刻調整を開始し、UACを承認したうえで、昇格ヘルパーのNTP取得または時刻適用を失敗させる。 | アプリは動作継続し、ずれ状態を維持し、失敗通知を表示する。 |

## NTP/時刻調整ログ

| ID | ケース | 手順 | 期待値 |
| --- | --- | --- | --- |
| LG-01 | 起動/セッションNTPログ | アプリを起動する。その後、アプリ起動中にログオンまたはロック解除を行う。 | `startup`、`logon`、`unlock` のいずれかのイベントが `TrayClockTooltip.log` に追記される。成功時は `OK` と `offset=... source=...`、失敗時は `FAILED error=ntp source=...` を記録する。 |
| LG-02 | 手動再取得ログ | トレイメニューの `NTP: refresh` を選択する。 | `refresh` イベントがログに追記される。NTP成功時のoffsetは符号付き、100秒未満は整数部2桁以上、小数部3桁固定で、例: `+00.842s`。 |
| LG-03 | 時刻調整ログ | `Adjust Windows time (admin)` を実行し、UACを承認する。 | 昇格ヘルパーが `adjust` イベントを追記する。成功時は `OK offset=... source=...`、失敗時は `FAILED` とエラー詳細を記録する。 |
| LG-04 | UACキャンセルログ | `Adjust Windows time (admin)` を開始し、UACをキャンセルする。 | キャンセルだけを理由に通知は出ない。非昇格側から `adjust CANCEL error=uac` がログに記録される。 |
| LG-05 | インストール先ログパス | `%LOCALAPPDATA%\Programs\TrayClockTooltip\TrayClockTooltip.exe` から起動し、NTP取得または時刻調整を行う。 | `%LOCALAPPDATA%\TrayClockTooltip\TrayClockTooltip.log` に出力される。自動起動登録を削除しても、このログは削除されない。 |
| LG-06 | ポータブルログパスとローテート | 上記以外のフォルダから起動し、NTP取得または時刻調整を行う。ローテート確認が必要ならログを256KB超まで増やす。 | 起動EXEと同じフォルダに出力される。256KBを超えた場合は `TrayClockTooltip.log.1` にローテートする。 |

## 通知ウィンドウ

| ID | ケース | 手順 | 期待値 |
| --- | --- | --- | --- |
| NF-01 | 通知テーマ | Windowsモードをダーク/ライトに切り替え、それぞれ通知を出す。 | 通知ウィンドウがWindowsモードに追従した色で表示される。 |
| NF-02 | NTP失敗通知クリック | NTP失敗通知をクリックする。 | 通知が閉じる。時刻調整は開始されない。 |
| NF-03 | 通知の差し替え | 通知表示中に別の通知を発生させる。 | 既存通知が閉じ、新しい通知が表示される。 |
| NF-04 | 通知言語 | 日本語のWindows UI環境と、日本語以外のWindows UI環境で各通知を表示する。 | Windows UI言語が日本語の場合、通知本文は日本語で表示される。日本語以外の場合、通知本文は英語で表示される。メニュー、ログ、インストール確認用メッセージボックスは英語のまま。 |

## テーマと表示

| ID | ケース | 手順 | 期待値 |
| --- | --- | --- | --- |
| TH-01 | ダークモード | Windowsモードをダークにし、フローティング時計を表示する。 | ダーク背景、明るい文字、ダーク用境界線で表示される。 |
| TH-02 | ライトモード | Windowsモードをライトにし、フローティング時計を表示する。 | ライト用のシステムInfo色で表示される。 |
| TH-03 | 起動中のテーマ変更 | アプリ起動中にWindowsモードを変更する。 | フローティング時計と通知が現在のWindowsモードに合わせて再描画される。 |
| TH-04 | 文字中央揃え | フローティング時計を表示する。 | タイムスタンプがウィンドウ内で縦横中央に表示される。 |
| TH-05 | 画面端 | 画面端付近でフローティング時計を表示する。 | フローティング時計が作業領域内に収まる。 |

## 要仕様確認

| ID | ケース | 現在の期待値 | 再検討理由 |
| --- | --- | --- | --- |
| OQ-01 | 標準ツールチップ抑止 | 標準ツールチップの内容は表示されない。Windows標準の通知センターを表示/非表示した後も、標準ツールチップではなくフローティング時計が表示される。 | トレイアイコンは `NIF_TIP` なしで登録し、`NIN_POPUPOPEN` / `NIN_POPUPCLOSE` 時だけ空tooltipをクリアする。Shell描画は環境依存。 |
| OQ-02 | トレイアイコン矩形取得失敗 | `Shell_NotifyIconGetRect` が失敗した場合、カーソル基準の位置にフォールバックする。 | フォールバック仕様として十分か要確認。 |
| OQ-03 | 隠れているインジケーター内 | 隠れているインジケーター内でもhoverでフローティング時計を表示する。 | Windowsのトレイ/フライアウト挙動により位置や自動非表示が変わる可能性がある。 |
| OQ-04 | タスクバー位置 | フローティング時計はトレイアイコン付近に出て画面内に収まる。 | 現状は下タスクバーを主想定。上/左/右タスクバーも仕様対象にするか要確認。 |
| OQ-05 | 複数モニター/DPI | トレイアイコン付近かつ作業領域内に表示される。 | 複数モニターや混在DPI環境で追加確認が必要。 |
| OQ-06 | しきい値未満の手動再取得 | ずれが現在の通知しきい値未満なら成功通知を表示する。 | 明示的なユーザー操作には結果を返しつつ、起動時/セッション通知時の成功は静かなままにする。 |
| OQ-07 | ずれ後のNTP失敗 | NTP失敗後はトレイメニューを `NTP: refresh` に戻し、以前のオフセットを破棄し、Adjustメニューを隠す。 | 古いずれ情報を消す仕様でよいか要確認。 |
| OQ-08 | トレイ右クリック時のhover | トレイ右クリック時、hoverプレビューを閉じてからメニューを開く。 | 標準ツールチップとは少し違うため好みの確認が必要。 |
| OQ-09 | hover消滅タイミング | 250ms間隔でマウス位置を確認して消す。 | 速すぎる/遅すぎる場合は調整余地あり。 |
| OQ-10 | 時刻フォーマット | 日付部分と時刻部分はWindowsのユーザー設定形式に従う。表示全体は `日付 時刻` の並び。Windowsの時刻形式が秒を含まない場合でも、秒は補完表示される。 | 海外環境や日付/時刻設定変更時の表示確認が必要。 |
| OQ-11 | 日付/時刻形式による幅 | フローティング時計の幅は、現在のWindows日付/時刻形式に対する最大想定サンプル文字列を基準に決まる。 | 海外環境や長いAM/PM表記で十分な余裕があるか確認が必要。 |

## Codex実施結果

実施日: 2026-06-13

実施範囲:

- デバッグ実行なし。
- `powershell -ExecutionPolicy Bypass -File .\src\build.ps1` によるビルド確認を実施。
- GUI操作、トレイ実操作、UAC操作、Windows設定変更、環境依存の視認確認はCodex側では未実施。
- 以下は、ビルド確認、`git diff --check`、ソーストレース、成果物確認、テストケース整合確認で再実施した最新結果のみ。
- 過去のCodex実施結果は破棄し、この結果欄を新規作成。

| 結果 | ID | 確認内容 |
| --- | --- | --- |
| PASS | ST-01, ST-02, ST-03, HV-01, HV-02, HV-03, HV-04, HV-05, HV-06, HV-07, HV-08, FL-01, FL-01A, FL-02, FL-03, FL-04, FL-05, FL-06, FL-07, FL-08, FL-09, TM-01, TM-01A, TM-02, TM-03, TM-04, TM-05, TM-06, TM-07, TM-08, TM-09, TM-10, TM-11, TM-12, TM-13, TM-14, FM-01, FM-02, FM-03, FM-04, FM-05, FM-06, FM-07, FM-08, FM-09, NT-01, NT-02, NT-03, NT-04, NT-05, NT-06, NT-07, AD-01, AD-01A, AD-02, AD-03, AD-04, AD-05, AD-06, AD-07, AD-08, AD-09, LG-01, LG-02, LG-03, LG-04, LG-05, LG-06, NF-01, NF-02, NF-03, TH-01, TH-02, TH-03, TH-04, TH-05, BS-01, BS-02, BS-03, OQ-01, OQ-02, OQ-06, OQ-07, OQ-08, OQ-09, OQ-10, OQ-11, SU-01, SU-02, SU-03, SU-04, SU-05, SU-06, SU-08, SU-08A, SU-09, SU-10, SU-10A, SU-11, SU-12, SU-13, SU-14 | ユーザー実操作、ソーストレース、ビルド、成果物確認で再実施。実施またはレビューしたケースにNGはありません。 |
| ソーストレースのみ | SU-15 | 1.2.0のしきい値挙動として追加。Codex側ではソーストレース/ビルドで経路確認済み。トレイ実操作での確認を推奨。 |
| ソーストレースのみ | NF-04 | 通知ローカライズとして追加。Codex側では言語選択経路をソーストレース/ビルドで確認済み。日本語/非日本語UI環境での実確認を推奨。 |
| ソーストレースのみ | NT-08, SU-01のインストール成功通知 | 起動確認とインストール引き継ぎ後の通知として追加。Codex側ではソーストレース/ビルドで経路確認済み。トレイ実操作での確認を推奨。 |
| 廃止 | SU-07 | 現行のインストール引き継ぎ/Startup登録ケースに置き換え。 |
| 未実施/保留 | OQ-03, OQ-04, OQ-05 | 隠れているインジケーター、下以外のタスクバー、複数モニター/混在DPIなど環境依存のため保留。 |

補足:

- ビルドは成功し、`dist\TrayClockTooltip.exe` を生成済み。`git diff --check` も問題なし。
- BS-02/BS-03をCodex側で実行。既存の `TrayClockTooltip` プロセスを停止してから実行し、ビルド済みEXEの起動を確認後、起動したプロセスを停止済み。
- 通知しきい値ロジックをソーストレースで再確認: コマンドラインの `--notify-threshold` を最優先し、なければStartup登録値を参照し、不正値は既定の1秒へフォールバックする。
- 通知しきい値メニューをソーストレースで再確認: `Notify threshold` はトレイメニューのみが持ち、チェック状態は現在値に追従し、フローティング時計メニューには表示しない。
- しきい値の永続化をソーストレースで再確認: 変更は現在プロセスへ即時反映され、Startup登録がある場合は登録済みEXEパスを維持したまま引数だけを書き換える。
- しきい値 `Off` をソーストレースで再確認: 自動のずれ通知は抑止し、NTP取得成功後のアプリ時計はNTP基準のまま、NTP時刻が利用可能ならShiftなしの通常メニューから時刻調整できる。
- インストール時READMEコピーをソーストレースで再確認: 起動元EXEの隣に `README.txt` / `README.ja.txt` が存在する場合だけインストール先へコピーし、存在しない場合もインストール失敗にはしない。
- フローティング側refresh削除を再確認: `ID_POPUP_REFRESH_NTP` は存在せず、`NTP: refresh` はトレイ側のみ。
- 標準ツールチップ抑止を再確認: トレイ登録は `NIF_TIP` なし、`NIN_POPUPOPEN` / `NIN_POPUPCLOSE` 時のみtooltip文字列をクリア。
- セッション通知によるNTP再取得を再確認: `WTS_SESSION_LOGON` / `WTS_SESSION_UNLOCK` で `StartNtpLoad` を呼び、`g_ntpQueryInProgress` で二重開始を抑止。
- 高度なShift時刻調整をソーストレースで再確認: トレイ/フローティングの両メニューが `CanForceAdjustmentFromMenu` を使用し、NTP問い合わせ中は条件から除外。
- 高度なログフォルダ表示をソーストレースで再確認: トレイ/フローティングの両Shiftメニューに `Open log folder` を追加し、強制Adjustの表示可否とは独立して表示する。
- NTP/時刻調整ログをソーストレースで再確認: 起動時、手動再取得、ログオン、ロック解除、UACキャンセル、昇格ヘルパーの時刻調整成功、昇格ヘルパーの時刻調整失敗の各経路で、通知やメニュー挙動を変えずにログイベントを追記する。
- ログ出力先をソーストレースで再確認: インストール先からの起動時は `%LOCALAPPDATA%\TrayClockTooltip`、それ以外はEXEと同じフォルダに出力し、自動起動登録削除ではログを削除しない。
- Startupメニューの接続をソーストレースで再確認: トレイメニューが `Startup` サブメニューを持ち、HKCU Run、登録先EXEの存在/バイナリ一致、インストール状態からグレーアウトを判定し、各コマンドからユーザー向けインストール、現在EXEの自動起動登録、自動起動削除ヘルパーを呼び出す。
- インストール後の起動引き継ぎをソーストレースで再確認: コピー後検証はバイト比較で行い、元プロセス終了前に内部的な親プロセス待ち引数付きでインストール先EXEを起動する。
- 開発用インストール確認をソーストレースで再確認: `--install-prompt` はインストール先EXEが存在し、現在EXEが別パスから起動している場合だけ確認を出し、インストール選択時は同じ検証付きインストール/再起動処理を使用する。
- ポータブル起動選択をソーストレースで再確認: `いいえ` 選択時は、グローバル単一起動ミューテックスでポータブル起動が黙って終了しないよう、起動中のインストール先/起動元パスの既存インスタンスへ通常終了を依頼してから起動を継続する。
- 既存インスタンス終了処理をソーストレースで再確認: インストール更新時はプロセスパスがインストール先EXEまたは起動元EXEと一致する起動中インスタンスにだけ `WM_CLOSE` を送り、短時間待ってからコピーする。強制終了は使用しない。
- ビルドスクリプトの起動補助をソーストレースで再確認。`-Run` / `-RunInstallPrompt` はGUIアプリを起動するため、この実施では未実行。
